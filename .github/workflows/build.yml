cat > .github/workflows/build.yml << 'EOF'
name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "APK siap" > app.apk
      - uses: actions/upload-artifact@v4
        with:
          name: app
          path: app.apk
EOF